%h1 Listing users

%table
  %thead
    %tr
      %th First name
      %th Last name
      %th Email
      %th Organization
      %th Role
      %th
      %th

  %tbody
    - @users.each do |user|
      %tr
        %td= user.first_name
        %td= user.last_name
        %td= user.email
        %td= user.organization.try(:name)
        %td= user.role
        %td= link_to 'Show', user
        %td= link_to 'Edit', edit_user_path(user)

%br
